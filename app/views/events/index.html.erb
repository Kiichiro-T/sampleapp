<h4><%= @user.name %>さんの作成したイベント</h4>
<% if @events.any? %>
  <% @events.each do |event| %>
    <p><%= link_to "#{event.name}", event_path(event) %></p>
  <% end %>
<% else %>
  <p>まだ作成したイベントはありません</p>
<% end %>
